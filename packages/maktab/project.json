{
  "name": "maktab",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/maktab",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "packages/maktab",
        "outputPath": "dist/packages/maktab"
      },
      "configurations": {
        "development": {
          "outputPath": "packages/maktab"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "maktab:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "maktab:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "maktab:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "maktab:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/maktab/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/maktab/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
